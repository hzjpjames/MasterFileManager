# MasterFileManager 项目介绍与构建进展

## 项目概述

**项目名称**: MasterFileManager  
**项目路径**: D:\MasterFileManager  
**框架**: React Native 0.73.6  
**目标平台**: Android APK  
**包名**: com.masterfilemanager  
**备份路径**: H:\bak\MasterFileManager_20260601_0936

### 项目功能
一个文件管理器 Android 应用，基于 React Native 开发，使用以下核心依赖：
- react-native-paper (UI 组件库)
- react-native-vector-icons (图标)
- react-native-reanimated (动画)
- react-native-gesture-handler (手势)
- react-native-screens (原生导航屏幕)
- react-native-video (视频播放)
- react-native-fs (文件系统)
- react-native-document-picker (文档选择)
- react-native-fast-image (图片加载)
- react-native-image-picker (图片选择)
- react-native-zip-archive (ZIP 压缩/解压)
- react-native-async-storage (本地存储)
- @react-navigation (导航)

---

## 构建环境

| 组件 | 版本/路径 |
|------|-----------|
| JDK | OpenJDK 17.0.13 (`F:\Android\jdk-17-fixed\jdk-17.0.13+11`) |
| Android SDK | `D:\Android\Sdk` (compileSdk 35) |
| NDK | 25.1.8937393 |
| Gradle | 8.3 |
| AGP | 8.1.4 |
| Node.js | QClaw 内置 v22.x |
| npm | npmmirror.com 镜像 |
| Gradle Wrapper | 腾讯云镜像 |
| Maven | 阿里云镜像 |

### 关键环境变量
```
JAVA_HOME=F:\Android\jdk-17-fixed\jdk-17.0.13+11
ANDROID_HOME=D:\Android\Sdk
```

### gradle.properties 关键配置
```
org.gradle.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=512m
android.enableJetifier=false
android.useAndroidX=true
```

---

## 构建进展

### ✅ 已完成
- Android 原生层编译全部通过（Java + C++ + Kotlin）
- JS Bundle 打包成功（`index.android.bundle`）
- 首次 Debug APK 构建成功（176.79 MB）

### ❌ 当前阻塞
**8GB 内存不足**，无法在本机完成完整构建流程（Metro bundle + Gradle + CMake 同时运行时 OOM）：
- 单独运行 Metro bundle：✅ 成功（停止 Gradle daemon 后）
- Gradle 构建到 `createBundleDebugJsAndAssets`：✅ 成功
- Gradle 构建到 `configureCMakeDebug`（reanimated C++ 编译）：❌ OOM

**解决方案**: 在另一台内存更大的电脑上构建，环境配置相同即可。

---

## 已解决的问题（共 18 项）

### 构建配置问题
1. **JAVA_HOME/bin 不在 PATH** → build_fixed.bat 设置 PATH
2. **Gradle 8.10.2 与 RN 0.73 不兼容** → 降级到 Gradle 8.3 + AGP 8.1.4
3. **native_modules.gradle 找不到 node** → node 命令改为绝对路径
4. **build_fixed.bat 中文编码乱码** → 重写为纯英文
5. **Gradle/Maven 下载慢** → 配置腾讯云 + 阿里云镜像

### 内存问题
6. **Gradle OOM** → gradle.properties 调整 -Xmx1536m -XX:MaxMetaspaceSize=512m
7. **Metaspace OOM** → build_fixed.bat 移除硬编码 JVM 参数
8. **Jetifier OOM** → 禁用 Jetifier (android.enableJetifier=false)
9. **8GB 内存构建 OOM** → 需要更大内存的机器

### 依赖兼容性问题
10. **react-native-video 旧 support 库** → patch build.gradle 排除旧依赖
11. **androidx.core 1.16.0 要求 AGP 8.6+** → 强制降级到兼容版本
12. **react-native-reanimated 3.6.1 要求 RN 0.78+** → 降级到 3.3.0
13. **react-native-gesture-handler 2.31.2 要求 RN 0.76+** → 降级到 ~2.14.0
14. **react-native-screens 3.37.0 要求 RN 0.76+** → 降级到 3.29.0

### 代码修复
15. **Flipper 未安装** → 注释 MainApplication.java 中的 Flipper 初始化
16. **reanimated Java 编译错误** → patch R.id.action_bar_root、isIdle()
17. **C++ hermes inspector 头文件缺失** → patch 用 __has_include 和 #if HERMES_ENABLE_DEBUGGER 保护

### 资源问题
18. **缺少 mipmap 图标** → 生成 ic_launcher.png
19. **缺少 debug.keystore** → keytool 生成

### JS Bundle 问题
20. **Metro 配置过时** → 更新 metro.config.js 使用 @react-native/metro-config
21. **react-native-paper 图标依赖缺失** → patch MaterialCommunityIcon.js 直接用 react-native-vector-icons
22. **@expo/vector-icons 引入 expo-modules-core 依赖链** → 移除，改用 react-native-vector-icons

---

## 已修改的关键文件

### 项目配置（已包含在备份中）
| 文件 | 修改内容 |
|------|----------|
| `android/build.gradle` | resolutionStrategy 强制低版本 androidx.core |
| `android/gradle.properties` | JVM 参数、禁用 Jetifier |
| `android/gradle/wrapper/gradle-wrapper.properties` | Gradle 8.3 + 腾讯云镜像 |
| `android/app/build.gradle` | debuggableVariants=[] (强制打包 JS bundle) |
| `android/app/debug.keystore` | 新生成的 debug 签名 |
| `build_fixed.bat` | 完整构建脚本（纯英文） |
| `metro.config.js` | 使用 @react-native/metro-config |

### node_modules patch（npm install 会覆盖，需重新 patch）
| 文件 | 修改内容 |
|------|----------|
| `node_modules/react-native-reanimated/android/build.gradle` | HERMES_ENABLE_DEBUGGER=0 |
| `node_modules/react-native-reanimated/Common/cpp/ReanimatedRuntime/ReanimatedHermesRuntime.h` | __has_include 保护 |
| `node_modules/react-native-reanimated/Common/cpp/ReanimatedRuntime/ReanimatedHermesRuntime.cpp` | #if HERMES_ENABLE_DEBUGGER 保护 |
| `node_modules/react-native-reanimated/android/src/main/java/.../ReanimatedKeyboardEventListener.java` | R.id/action_bar_root + isIdle() 修复 |
| `node_modules/react-native-video/android/build.gradle` | 排除旧 support 库 |
| `node_modules/react-native-paper/lib/commonjs/components/MaterialCommunityIcon.js` | 直接用 react-native-vector-icons |
| `node_modules/react-native-paper/lib/module/components/MaterialCommunityIcon.js` | 同上 |
| `node_modules/@react-native-community/cli-platform-android/native_modules.gradle` | node 绝对路径 |

### 自动生成（无需备份）
| 文件 | 说明 |
|------|------|
| `android/app/src/main/assets/index.android.bundle` | JS bundle（构建时自动生成） |
| `android/app/src/main/res/mipmap-*` | launcher 图标 |
| `android/app/build/` | 构建输出（重建即可） |

---

## 在新电脑上构建的步骤

### 1. 环境准备
确保新电脑有：
- JDK 17（设置 JAVA_HOME）
- Android SDK（compileSdk 35, NDK 25.1.8937393）
- Node.js 18+ 和 npm
- 建议内存 ≥ 16GB

### 2. 恢复项目
```bash
# 从备份复制到工作目录
# 然后：
cd MasterFileManager
npm install
```

### 3. ⚠️ 必须应用 node_modules patch（否则构建必失败）

`npm install` 后，node_modules 中的 7 个文件**必须手动修改**，否则编译 100% 报错：

#### Patch 1: reanimated build.gradle — 禁用 Hermes 调试器
文件: `node_modules/react-native-reanimated/android/build.gradle`
找到 `HERMES_ENABLE_DEBUGGER` 相关行，将值改为 `0`（或添加 `-DHERMES_ENABLE_DEBUGGER=0` 到 cFlags/cppFlags）

#### Patch 2: ReanimatedHermesRuntime.h — __has_include 保护
文件: `node_modules/react-native-reanimated/Common/cpp/ReanimatedRuntime/ReanimatedHermesRuntime.h`
将 `#include <hermes/inspector/chrome/Registration.h>` 改为：
```cpp
#if __has_include(<hermes/inspector/chrome/Registration.h>)
#include <hermes/inspector/chrome/Registration.h>
#endif
```

#### Patch 3: ReanimatedHermesRuntime.cpp — 条件编译保护
文件: `node_modules/react-native-reanimated/Common/cpp/ReanimatedRuntime/ReanimatedHermesRuntime.cpp`
将 inspector 相关的 `#include` 和使用 inspector 的代码块用 `#if HERMES_ENABLE_DEBUGGER` ... `#endif` 包裹

#### Patch 4: ReanimatedKeyboardEventListener.java — 兼容性修复
文件: `node_modules/react-native-reanimated/android/src/main/java/com/swmansion/reanimated/keyboard/ReanimatedKeyboardEventListener.java`
- `R.id.action_bar_root` → 改为 `android.R.id.content`
- `isIdle()` → 改为 `isIdleAndNotYetInQueue()`（或根据具体 API 调整）

#### Patch 5: react-native-video build.gradle — 排除旧 support 库
文件: `node_modules/react-native-video/android/build.gradle`
在 dependencies 中添加 `exclude group: 'com.android.support'`

#### Patch 6 & 7: react-native-paper MaterialCommunityIcon.js — 直接用 react-native-vector-icons
文件1: `node_modules/react-native-paper/lib/commonjs/components/MaterialCommunityIcon.js`
文件2: `node_modules/react-native-paper/lib/module/components/MaterialCommunityIcon.js`
将 `loadIconModule` 函数改为直接 require `react-native-vector-icons/MaterialCommunityIcons`，去掉对 `@react-native-vector-icons/material-design-icons` 和 `@expo/vector-icons` 的 try/catch（这两个包在纯 RN 项目中不存在，会导致 Metro bundle 失败）：
```js
const loadIconModule = () => {
  try {
    return require('react-native-vector-icons/MaterialCommunityIcons').default;
  } catch (e) {
    return null;
  }
};
```

**强烈建议**：使用 `patch-package` 将以上 patch 持久化，这样 `npm install` 后只需 `npx patch-package` 即可恢复，不用每次手动改。

### 4. 构建
```bash
cd android
gradlew assembleDebug
```
APK 输出: `android/app/build/outputs/apk/debug/app-debug.apk`

### 5. 安装测试
```bash
adb install app-debug.apk
```

---

## 备份信息

- **备份时间**: 2026-06-01 09:36
- **备份路径**: H:\bak\MasterFileManager_20260601_0936
- **备份大小**: 84 MB（排除 node_modules、build、.gradle）
- **包含**: 源码、配置文件、已生成的 JS bundle、debug keystore、构建脚本
- **不包含**: node_modules（需 npm install 恢复）、build 输出

### 为什么备份只有 84MB 而 APK 有 176MB？
- **APK 176MB** 包含：编译后的原生 .so 库（arm64-v8a + armeabi-v7a + x86_64 三个架构）、hermes 引擎、所有 Java/Kotlin 字节码等
- **备份 84MB** 排除了：
  - `node_modules/`（~300-500MB，通过 `npm install` 从 package.json 恢复）
  - `android/app/build/`（编译产物，重新构建即可生成）
  - `android/.gradle/`（Gradle 缓存，自动下载）
- 备份中 81MB 是 `bin/node.exe`（构建用包装器），真正源码+配置约 3MB
- **经逐文件对比验证，源码和配置文件与原项目完全一致，无遗漏** ✅

---

*最后更新: 2026-06-01 09:49*
